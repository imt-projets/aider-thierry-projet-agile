{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run clean && npm run build && node dist/index.js",
    "build": "tsc",
    "clean": "rimraf dist",
    "watch": "tsc -w",
    "dev": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2"
  }
}
